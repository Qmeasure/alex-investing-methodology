# 从一个未取整的浮点数，反推 Claude 订阅套餐的真实用量上限

> **一句话总结**
>
> 通过 Claude 接口返回的未取整浮点数（如 `0.16327272727272726`），利用数论中的 Stern-Brocot 树和最小公倍数，可以精确还原出 Claude 各订阅套餐的真实用量上限——并由此发现，**订阅套餐相比 API 定价最高可便宜 36 倍**。

---

## 第一部分：Claude 套餐的真实额度

### 各套餐的真实数字

Anthropic 从未公开过 Claude 订阅套餐的精确用量上限。但通过逆向工程，可以还原出如下数据：

| 套餐 | 每 5 小时额度（credits） | 每周额度（credits） |
|---|---|---|
| Pro（$20/月） | 550,000（1×） | 5,000,000（1×） |
| Max 5×（$100/月） | 3,300,000（6×） | 41,666,700（8.33×） |
| Max 20×（$200/月） | 11,000,000（20×） | 83,333,300（16.67×） |

两个关键发现：

- **Max 5× 是性价比最高的套餐。** 它实际提供了 Pro 的 6 倍会话额度（而不是名义上的 5 倍），以及 8.33 倍的周额度。它在「超额兑现」承诺。
- **Max 20× 没有想象中划算。** 虽然 5 小时会话限制确实是 Pro 的 20 倍，但每周总用量只有 Max 5× 的 2 倍——花了 2 倍的钱，只多干了 2 倍的活，而不是 4 倍。那个「20×」后面的星号在拼命工作。

### 对比 API：订阅套餐碾压

按 Opus 模型的 API 定价折算：

| 套餐 | 月费 | 等效 API 成本 | 倍率 |
|---|---|---|---|
| Pro | $20 | ~$163 | 8.1× |
| Max 5× | $100 | ~$1,354 | 13.5× |
| Max 20× | $200 | ~$2,708 | 13.5× |

而且这还是保守估计。

### 缓存读取免费：隐藏的巨大优势

API 计费中，缓存命中（cache read）仍要按输入价格的 10% 收费。但在订阅套餐中，**缓存读取不消耗任何额度**。

这在 Claude Code 等 Agent 场景下意义重大。智能体每一轮工具调用后都会重新读取整个上下文——100K tokens 的上下文，每次都要付 10% 的输入费，而订阅用户不用付一分钱。

两类典型场景对比：

| 场景 | 组成 | 套餐消耗 | 单次 API 花费 | 按周额度换算 | 等效 API 月成本 | 价值比 |
|---|---|---|---|---|---|---|
| 冷缓存 | 100K 缓存写入 + 1K 输出 | 70,000 credits | $0.65 | 约 595 次/周 | 约 $1,676/月 | 16.8× |
| 热缓存 | 100K 缓存读取 + 1K 新写入 + 1K 输出 | 4,000 credits（缓存读取免费） | $0.08 | 10,416 次/周 | 约 $3,667/月 | 36.7× |

> 你在热缓存场景下只付了 $100，却拿到了等效 $3,667/月 的 API 价值。

---

## 第二部分：「Credits」到底是什么？

Credits 是 Anthropic 内部追踪用量的计费单位。这个值不出现在任何公开 API 字段中，但它是所有限额的底层度量。

计算公式：

```
消耗的 credits = ⌈ 输入tokens × 输入费率 + 输出tokens × 输出费率 ⌉
```

不同模型的费率：

| 模型 | 输入费率（credits/token） | 输出费率（credits/token） |
|---|---|---|
| Haiku | 2/15 ≈ 0.133 | 10/15 ≈ 0.667 |
| Sonnet | 6/15 = 0.4 | 30/15 = 2.0 |
| Opus | 10/15 ≈ 0.667 | 50/15 ≈ 3.333 |

数字看起来有点奇怪，但比例与 API 定价完全一致：**输出是输入的 5 倍，Opus 是 Haiku 的 5 倍。**

---

## 第三部分：怎么从浮点数反推出精确限额？

这是最精彩的部分——一套漂亮的数学方法，从浮点数中反推出精确整数。

### 起点：一个可疑的浮点数

Claude.ai 官方的 `/usage` 端点返回的用量是四舍五入到百分比的数字，没什么信息量。但在 Max 5× 账户的 SSE 生成响应中，用量数值是未取整的双精度浮点数：

```
0.16327272727272726
```

这看起来像某个分数被转换成了小数。如果能还原这个分数，分母就可能是额度上限（因为 `用量 = 已用额度 / 总额度`）。

### 第一步：理解浮点数的精度区间

IEEE-754 双精度浮点数有 52 位尾数。在 0–1 范围内，精度约为 10⁻¹⁷。

这意味着什么？每个浮点数并不精确表示一个实数，而是代表了一个极窄的区间 `[L, U)`。所有落在这个区间内的有理数，都会被舍入到同一个浮点值。

原始分数一定在这个区间内。目标是：**在这个宽度仅为 10⁻¹⁷ 的区间中，找到最简分数。**

为什么找「最简」？因为 `0.16327272727272726` 当然可以写成 `16327272727272726 / 100000000000000000`，但这显然不是原始分数——没有人会把额度上限设成 100 万亿。原始分数的分母应该是一个合理的数字。

### 第二步：Stern-Brocot 树——高效搜索最简分数

Stern-Brocot 树是一棵特殊的二叉搜索树。它的核心特性是：**按分母从小到大的顺序遍历所有正有理数。** 这正好满足「找最简分数」的需求。

算法过程：

1. 初始化左边界 `0/1`，右边界 `1/0`（代表无穷大）
2. 计算中间分数（mediant）：分子相加 / 分母相加，即 `(a+c)/(b+d)`
3. 比较中间分数与目标区间 `[L, U)`：
   - 中间分数 ≥ U → 用它替换右边界，向左搜索
   - 中间分数 < L → 用它替换左边界，向右搜索
   - 中间分数落在 `[L, U)` 内 → 命中，这就是最简分数

以 `0.16327272727272726` 为例，搜索过程如下（简化版）：

```
step 00: mediant=1/1   → ≥ U, 向左
step 01: mediant=1/2   → ≥ U, 向左
...
step 05: mediant=1/6   → ≥ U, 向左
step 06: mediant=1/7   → < L, 向右
...
step 14: mediant=9/55  → ≥ U, 向左
...
step 69: mediant=449/2750 → 命中！
```

验证：`449 ÷ 2750 = 0.16327272727272726` ✓

Stern-Brocot 树之所以有效，是因为 mediant 运算天然保证分母较小的分数先被找到。在一个宽度仅 10⁻¹⁷ 的区间内，能命中的简单分数通常只有一个——这就排除了歧义。

### 第三步：最小公倍数（LCM）——锁定真实上限

单次采样得到的分数是约分后的结果。比如 `449/2750`，真实情况可能是 `449000/2750000`。分母 2750 只是真实上限的一个因子。

解决方案：**多次采样，在不同用量水平下获取多个分母，然后取它们的最小公倍数（LCM）。**

核心逻辑：

- 每次采样得到的分母 d，真实上限 D 必须能被 d 整除（因为 `已用/D` 约分后得到 `?/d`，说明 `D = k × d`）
- 多个 d 的 LCM 只能增长，不会超过 D
- 当 LCM 在多次采样后不再变化，它就几乎确定等于 D

实际例子：两次采样分别得到分母 `2750` 和 `75000`，LCM = `3,300,000`——这正是 Max 5× 套餐的 5 小时额度上限。

### 第四步：反推 credits 与 token 的换算关系

模型费率（不同模型每 token 消耗多少 credits）是通过大量数据采集和假设验证得出的。记录不同模型、不同输入输出长度下的 credits 消耗，建立方程组，最终拟合出前面那张费率表。所有数字都经过了精确验证。

---

## 结论与建议

### 侧信道泄漏无处不在

> 仅仅因为两个浮点数忘了取整，整个内部定价体系就被完整还原了。

这是一个经典的侧信道案例——敏感信息不是通过正面突破获取的，而是通过看似无害的数值精度泄露的。

### 实用建议

- **能用订阅就不要用 API。** 价值差距是数量级的，尤其在 Agent 场景下，缓存读取免费这一项就足以拉开巨大差距。
- **Max 5× 是甜蜜点。** 真实额度远超名义承诺，性价比在所有套餐中最优。
- **缓存是关键变量。** 在 Claude Code 等长上下文 Agent 场景中，缓存读取占据了绝大部分 token 消耗。订阅套餐对缓存读取完全免费，这一条规则让套餐的真实价值在热缓存场景下膨胀到 API 的 36 倍以上。
- **唯一例外：** 如果因为企业 / 团队管理等原因必须走 API（采购流程、合规要求等），那么上述对比的参考意义会降低。
