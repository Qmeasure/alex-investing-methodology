# Alex-投资方法论 · Obsidian 投资母 Vault

本目录是 Obsidian vault + git 仓库。任何批量修改前先 `git add -A && git commit` 打快照。

根目录 `CLAUDE.md` 是本文件的同步副本(逐字一致):改动本文件后必须同步更新 `CLAUDE.md`。

## 目录归属

| 内容 | 去处 |
|---|---|
| 投资方法论文章(转 md 的文章/研报/访谈/观点) | `方法论/`(夹内扁平,**不建子文件夹**) |
| 非投资内容 | `杂记/` |
| 概念枢纽页 | `concepts/` |
| 公司研报、估值模型产出(equity-research 等 skill) | `research/公司研究/{公司}/` |
| 图片附件 | `attachments/`,正文用 `![[文件名]]` 嵌入 |

检索靠 frontmatter + `bases/方法论总览.base`(嵌在 [[Home]]),不靠文件夹分类。Bases 视图自动更新,入库不用动它。

## 新增笔记 SOP(每篇必做)

### 1. 转 Markdown(外来文章)

- 全覆盖、尽量保留原文,但要结构化(标题层级/表格/列表/引用块)
- 文件名自拟、与主题相关;作者和日期**原文可考才标注**,不臆造;相对日期转绝对
- **图片是重点**:把图里的数据、逻辑、结论融进正文论证(数字焊进句子),不搞"观点一段 + 图注一段"两张皮;PDF 图表渲染 PNG 逐张读数,目测近似值标"约"
- 剔除样板内容:问候语、公众号推广、券商免责声明、目录页;作者本人结语保留
- 大文件(合集/多页研报)写 Python 脚本批量转,保证全覆盖

#### DataCharts 图表（外部材料入库时）

- 原 Markdown 表格是**唯一数据源**。仅当表格首列是时间或分类、第二列有至少 3 行纯数值（`-?数字` 或小数；不含 `%`、货币/时间单位、逗号、`—`、`约`、`100%+` 等）时，在表格正下方加 DataCharts 图块；时间序列用 `line`，分类比较用 `bar`。
- 图块必须用 `source(...) :: <首列>[0,1] is table from <Vault 相对路径（不带 .md）>` 直接读取原表；加 `obj.scales.x.type = category`。不得复制数值、不得改写原表以迎合图表。
- 每张图必须明确写入：`obj.plugins.title.text`（图表标题）、横轴和纵轴的 `title.text`，以及单位。时间轴标时间口径；数值轴在名称内写单位，例如“市盈率（TTM，倍）”“指数（点）”。图块放在源表正下方；标题、轴标题和刻度沿用 DataCharts 的 Vault 统一视觉参数。
- 不合格表格保留表格本身，不能自动图表化或擅自清洗数据。需要图表时，先请 Alex 确认口径与数值规范化方案。
- 当前 DataCharts 含本地“首条数据行”修复，见 `.obsidian/plugins/datacharts/LOCAL_PATCH.md`；更新插件前必须确认该修复未被覆盖。
- 入库自检增加：每个图块的源文件/表头/列序号可解析，图表行数与原表相同且首条数据不丢失；在 Obsidian 阅读视图实际查看渲染。

### 2. frontmatter 六字段(模板)

```yaml
---
type: 方法论        # 方法论/交易性机会/复盘/行业认知/人物观点/杂记/概念/索引
author:            # 原文作者或机构,没写明留空
date:              # 原文日期 YYYY-MM-DD,可考才填
tags:              # 只从 标签规范.md 的受控词表选,1-3 个
  - 估值
aliases: []        # 英文原名、常用简称
rating:            # 留空,由 Alex 自评 1-5
---
```

type 判定口径和 24 词受控词表见 [[标签规范]]。**tags 不许自造新词**;确需新词时先确认现有词覆盖不了,再加词并同步更新 [[标签规范]]。

### 3. 尾部「## 相关」区(双链,正文一字不动)

每篇末尾追加,格式:

```markdown
## 相关

- 概念:[[逆向投资]] · [[市场底部]]
- [[另一篇笔记]] — 呼应:一句话说明为什么相关
```

规则:

- **概念链 ≥1 条**(零孤岛铁律),从 `concepts/` 现有 36 页里选,读完文章按核心论点挑
- **文对文直链**只收三类强关系:**观点呼应 / 观点对立 / 同一事件互补**,每条带一句理由,上限 5 条;同标签不构成直链理由。加直链必须**双向**——对方笔记的相关区也要补一条指回来
- 伟志思考专栏新篇:加`- 上一篇:[[…]] · 下一篇:[[…]](伟志思考专栏,总目录见 [[伟志思考-专栏索引]])`,并把该篇登记进 [[伟志思考-专栏索引]] 对应年份;属「投资中最困难的事」系列的再加系列链并更新 [[伟志思考-投资中最困难的事系列索引]]

### 4. 新概念页(仅当确有必要)

某个概念在 ≥2 篇文章反复出现、且现有 36 页覆盖不了时:在 `concepts/` 建页(type: 概念,3-5 句定义 + 概念互链 + 结尾"反链聚合"说明,格式抄现有页),并登记进 [[标签规范]]。细粒度概念只建概念页、**不进 tags 词表**。

### 5. Canvas(可选)

`投资方法论体系.canvas` 是五环节决策流程图(认知与体系→估值与选股→周期与择时→交易执行→复盘与案例库)。只有新增了**概念页**或**够格当代表作**的文章才需要加节点(JSON Canvas 1.0,16 位 hex 节点 id,file 节点路径填 `方法论/xxx.md`);普通文章不用上图,经概念页反链可达。

## 入库后自检

1. YAML 可解析、六字段齐全、type 在枚举内、tags ⊆ 词表
2. 所有 `[[链接]]` 目标存在(注意 `.base`/`.canvas` 与 aliases 也是合法目标;反引号内的示例不算链接)
3. 本篇 ≥1 条概念链;新加直链两端互见
4. 若动了概念页/索引页,确认无坑链
5. `git commit`
6. `git push`(本地提交不等于入库完成,必须推到远端;批量入库多篇后再一次性 push 也可以,但当次会话结束前必须确认 `git status` 显示 `working tree clean` 且分支与 `origin/main` 一致)

## 禁止事项

- 不改动已入库文章的正文(链接只进相关区)
- 不在 `方法论/` 内建子文件夹;新专栏系列用文件名前缀 + 系列 tag + 索引页承载
- 不往 tags 里塞词表外的词;不给 rating 代填
- 不用 Markdown 链接做 vault 内互链(一律 wikilink;外部 URL 才用 Markdown 链接)
